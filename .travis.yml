language: python

python:
  - 2.7

env:
  - DJANGO=1.9
  - DJANGO=1.10

install:
  - pip install django-fake-model
  - pip install coveralls

script:
  - python manage.py test mqueue
  - coverage run --source=mqueue

after_success:
  coveralls
