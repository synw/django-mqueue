language: python

python:
  - 2.7

env:
  - DJANGO=1.9
  - DJANGO=1.10

install:
  - pip install django-fake-model
  - pip install pytest pytest-cov coveralls

script:
  py.test mqueue/tests.py --cov=mqueue mqueue

after_success:
  coveralls
